export const COLORS = {
  primary: '#F87060',
  secondary: '#102542',
  white: '#FFFFFF',
  black: '#000000',
  gray: '#CCCCCC',
};

export const FONTS = {
  // We will load the Poppins font later
  // For now, use system fonts
  h1: { fontSize: 28, fontWeight: 'bold' },
  body: { fontSize: 16 },
};

const theme = { COLORS, FONTS };
export default theme;
